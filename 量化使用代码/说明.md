# 迷你硬币主脑量化使用说明（可复现版）

> 归档日期：2026-08-11（复盘 v11b 量化流程后整理）
> 目标：在新机器上**复制粘贴即可复现**从 checkpoint → Q8 GGUF → 启动推理的全流程

---

## 〇、前提条件（输入物）

量化的**输入**是训练好的 HF 格式 checkpoint，一个目录里必须包含：

```
ckpt_tool_main_v12d/
├── config.json          # 模型架构配置
├── model-*.safetensors  # 权重分片
├── tokenizer.json       # tokenizer（Qwen3 用 tiktoken 格式）
├── vocab.json / merges.txt（或 tokenizer.model）
└── 可选：chat_template.jinja / added_tokens.json / generation_config.json
```

> 路径在下面的命令里用 `${CKPT_DIR}` 占位，换成你的实际路径。

---

## 一、环境搭建（新机器，一次性）

### 1. llama.cpp（量化 + 推理工具）

```bash
# 获取源码（v11b 用的是 quant_version=3 的新版；clone 最新稳定版即可，qwen3 支持稳定）
git clone https://github.com/ggml-org/llama.cpp ~/llama.cpp
cd ~/llama.cpp

# 量化只需要 convert_hf_to_gguf.py（纯 Python，CPU 跑，无需编译）
# 推理需要编译 llama-server（GPU 版；若机器无 N 卡可去掉 -DGGML_CUDA=ON 编译 CPU 版）
cmake -B build -DGGML_CUDA=ON
cmake --build build --config Release -j $(nproc)
```

### 2. Python 环境（convert 脚本依赖）

```bash
# 新建量化专用 venv（convert 只需 numpy + torch，不需要完整训练环境）
python3 -m venv ~/q_venv
source ~/q_venv/bin/activate
pip install numpy torch
# 若 convert 报缺 transformers/sentencepiece，再补：pip install transformers sentencepiece
```

> 老机器已有 `~/mini_coin_venv`（训练环境，torch/numpy 齐全）可直接用它，不用新建。

---

## 二、量化（核心步骤，≈10 分钟，产物 ~8.7GB）

```bash
# 变量（换成你的实际路径）
CKPT_DIR=/mnt/c/Users/Autogram-coin/Desktop/ckpt_tool_main_v12d   # checkpoint 目录
OUT_FILE=/tmp/coin_v12d_q8.gguf                                    # 输出 GGUF

source ~/q_venv/bin/activate   # 或 ~/mini_coin_venv
cd ~/llama.cpp && python3 convert_hf_to_gguf.py \
  "$CKPT_DIR" \
  --outfile "$OUT_FILE" \
  --outtype q8_0
```

**预期产物 metadata（v11b 实测值，v12d 应同构）**：
- 架构 qwen3 / 36 层 / 4096 维 / 32 头 / context 40960
- tokenizer: model=gpt2, pre=qwen2, eos=151645, bos=151643
- 文件大小 ≈8.7GB = Q8_0（quant_version=3）

**验证命令**（读 GGUF metadata）：
```bash
cd ~/llama.cpp && PYTHONPATH=~/llama.cpp/gguf-py python3 -c "
from gguf import GGUFReader
r = GGUFReader('$OUT_FILE')
for k in ['general.architecture','qwen3.block_count','qwen3.context_length','tokenizer.ggml.pre','tokenizer.ggml.eos_token_id']:
    print(k, '=', r.fields[k].contents())
"
```

---

## 三、模板修正（重要）

### 背景

v11b 量化时的 `/tmp/coin_template.jinja` **缺少 `memory→记忆` 的 role 替换**（训练侧模板有）：
- 旧版：只有 `user→输入`、`assistant→硬币`
- 正确版：另有 `memory→记忆`

**实际影响：无**。管线调用链是 `pipeline build_prompt（字符串）→ 18771 LLM桥 → llama-server /completion（raw prompt）`，模板只对 messages API 生效，管线完全不经过模板。

**但已改对**：本文件夹的 `coin_template.jinja` 是修正版（`\n` 转义风格 + 完整三个替换）。

### 使用

```bash
cp /mnt/c/Users/Autogram-coin/Desktop/新架构硬币/量化使用代码/coin_template.jinja /tmp/coin_template.jinja
```

> 训练侧原版模板（字面换行风格，277B）在 `Desktop/ckpt_tool_main_v11b/chat_template.jinja`，两者渲染结果一致，本文件夹版本为 llama-server 兼容的 `\n` 转义写法。

---

## 四、启动推理（llama-server）

```bash
cd ~/llama.cpp
./build/bin/llama-server \
  --model /tmp/coin_v12d_q8.gguf \
  --jinja --chat-template-file /tmp/coin_template.jinja \
  --n-gpu-layers 99 --host 127.0.0.1 --port 8080
```

**验证**（另开终端）：
```bash
# raw prompt 方式（管线实际用法）
curl -s http://localhost:8080/completion -H "Content-Type: application/json" \
  -d '{"prompt":"<|im_start|>输入\n你好<|im_end|>\n<|im_start|>硬币\n","n_predict":50}'

# messages API 方式（验证模板修正生效：role=memory 应渲染成"记忆"）
curl -s http://localhost:8080/v1/chat/completions -H "Content-Type: application/json" \
  -d '{"messages":[{"role":"memory","content":"测试记忆"},{"role":"user","content":"你好"}],"n_predict":30}'
```
第二个请求若模板生效，prompt 里应出现 `<|im_start|>记忆`。

---

## 五、整合进 start_all.sh

替换模型路径（原 v11b → v12d）：
```bash
# 原：-m /tmp/coin_v11b_q8.gguf
# 改：-m /tmp/coin_v12d_q8.gguf
```

---

## 六、相关文件位置速查

| 内容 | 位置 |
|------|------|
| llama.cpp（工具） | WSL `~/llama.cpp/`（编译产物 `build/bin/`） |
| Python 环境 | `~/q_venv`（或 `~/mini_coin_venv`） |
| v11b safetensors | `Desktop/ckpt_tool_main_v11b/`（含训练侧模板 chat_template.jinja） |
| v11b Q8 GGUF | `/tmp/coin_v11b_q8.gguf`；备份 `Desktop/模型备份8月2日/主脑/coin_v11b_q8.gguf` |
| v12d safetensors | `Desktop/ckpt_tool_main_v12d/` |
| 修正版模板 | 本文件夹 `coin_template.jinja` |
| 量化输出 | `/tmp/coin_v12d_q8.gguf` |
