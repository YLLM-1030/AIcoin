# 上文记忆 — 架构说明

> 最后更新: 2026-08-02
> 状态: 已实测跑通（L0→L1→L2→L3→L4 全链路 + 视图 UI）

---

## 概述

模拟人类记忆的层级化压缩机制。对话从高保真原文逐步压缩为抽象摘要，通过多层过滤只保留不平常的信息到永久记忆。

---

## 神经科学基础

| 概念 | 来源 | 对我们系统的含义 |
|------|------|----------------|
| 工作记忆容量 4±1 chunk | Cowan 2001 | L0 原文保留 5 轮 |
| 逐字→要点→精髓 逐层转化 | Nature Communications 2021 | 每层丢掉不同粒度的信息 |
| 睡眠期系统巩固（选择性保留） | Born & Wilhelm 2012 | PPL 过滤：低信息量内容丢弃 |
| 记忆的语义化（细节→概念） | Lifanov et al. 2021 | L1 保留概念，L2 保留 gist |
| 多痕迹理论（永久记忆依赖海马体） | Nadel & Moscovitch | L4 永久记忆需要检索机制 |

---

## 四层记忆模型

```
L0 (原文)        5 轮完整对话
   ↓ 5→1 摘要化 (纯文本 PPL 过滤)
L1 (段落摘要)    每条 = 1 轮压缩, 带 raw_ppl
   ↓ 满 6 条合并最老 3 条 (PPL ≥ 5 才保留)
L2 (会话摘要)    每条 = 3 个 L1 合并, 带时间戳
   ↓ 满 6 条合并最老 3 条
L3 (日常摘要)    代码拼装：我和{参与者}进行了闲聊
   ↓ 直接归档
L4 (永久记忆)    来自 L3 + L1 高 PPL(>15) 条目
```

### 各层存储格式

| 层级 | 存储格式 | 示例 |
|------|---------|------|
| L0 | `[("user", 原文), ("assistant", 原文), ...]` | 5 轮原始对话 |
| L1 | `[(摘要文本, raw_ppl), ...]` | `("螺丝说弄丢了钱...", 126.45)` |
| L2 | `[(摘要文本, 时间戳), ...]` | `("**主题**：拉姆的感情状况...", "07-29 20:21")` |
| L3 | `[(摘要文本, 开始时间, 结束时间), ...]` | `("我和清风明月、螺丝进行了闲聊", "07-29 22:14", "07-29 22:14")` |
| L4 | `[(文本, 类型, 时间), ...]` | `("螺丝说弄丢钱...", "重要事件", "07-29 21:47")` |

### 触发条件（与实际代码一致）

| 转换 | 条件 | 处理 |
|------|------|------|
| L0→L1 | history > 10 (5轮) | 取最早未处理的一轮生成摘要 + raw_ppl |
| L1→L2 | mem_l1 ≥ 6 | 取最老 3 条，过滤 PPL < 5，调 4B 合并为 1 条 L2 |
| L2→L3 | mem_l2 ≥ 6 | 取最老 3 条，调 4B 总结参与者 |
| L1→L4 | raw_ppl > 15 | 直接复制到 L4，标记"重要事件" |
| L3→L4 | 每次生成 L3 时 | 直接复制到 L4，标记"日常" |

### PPL 过滤策略

| 层级 | PPL 阈值 | 说明 |
|------|---------|------|
| L1 存储 | 不过滤 | 全部标注 PPL |
| L1→L2 | PPL ≥ 5 | 低于 5 的日常内容丢弃 |
| L1→L4 | PPL > 15 | 极高 PPL 直接进永久 |

PPL 由 `calc_ppl()` 独立计算：将摘要文本单独发给 4B 模型，`n_predict=0` 只计算每个 token 的 logprob 均值，再取 exp(-mean)。与上下文无关，反映摘要文本本身的信息量。纯文本 PPL 正常范围 2~30+，>5 即有信息量，>15 为异常事件。

---

## 各层 Prompt

### L1 L0→L1（段落摘要）— 已实测验证

发给摘要模型（Qwen3.5-4B, 18773）的完整 prompt：

```
<|im_start|>system
用第一人称把一段对话压缩成一句话。只压缩，不解释。
<|im_end|>
<|im_start|>user
{name}对我说：{用户文本}
我想：{内心思考，来源名已替换为"有人说"，已移除[TOOL:]标记}
{动作描述，如"我故意报错"或"我说：xxx"}
<|im_end|>
<|im_start|>assistant
<think>
</think>
```

预处理流程：
1. `对话[来源名]：xxx` → `{来源名}对我说：xxx`（强调对方在跟我对话）
2. `我想：` 中的来源名 → `有人说`（避免模型混淆用户名和名词）
3. `[TOOL:报错]xxx` → `我故意报错`
4. `[TOOL:回复]xxx` → `我说：xxx`
5. `[TOOL:搜索]xxx` → `我搜索：xxx`
6. `[TOOL:表情]` / `[TOOL:结束]` → 跳过

### L2 L1→L2（主题摘要）— 已实测验证

```
<|im_start|>system
你是一个记忆管理器。找到以下对话中最重要的一个主题，并且说出谁支持这个主题，谁反对这个主题。你只需要输出主题，以及支持者和反对者。不需要说明支持的理由，也不用输出其他东西。
<|im_end|>
<|im_start|>user
{5条L1摘要}
<|im_end|>
<|im_start|>assistant
<think>
</think>
```

触发规则：L1 满 6 条 → 取最老 3 条 → PPL 过滤（<5 丢弃）→ 合并为 1 条 L2 → 移除已处理的 3 条。

### L3 L2→L3（日常摘要）— 代码拼装

不调模型，直接提取 L2 条目中"支持者/反对者"字段里的名字，拼接为：

```
我和{参与者列表}进行了闲聊
```

参与者提取：每行找"支持者"或"反对者"，取后面的内容，用标点 `、，,：:；;（）()【】[]{}——\s*` 分割，过滤掉"无""我""支持者""反对者""主题"后去重。

### L4（永久记忆）— 直接归档

| 来源 | 条件 | 标记 |
|------|------|------|
| L1 | raw_ppl > 15 | "重要事件" |
| L3 | 每次生成 L3 时 | "日常" |

---

## 摘要模型

| 模型 | 参数量 | 量化 | 体积 | 端口 |
|------|--------|------|------|------|
| Huihui-Qwen3.5-4B-abliterated | 4B | Q4_K_M | ~2.8GB | 18773 |

启动方式：

```bash
~/llama.cpp/build/bin/llama-server \
  -m /mnt/c/Users/Autogram-coin/Desktop/huihui-qwen3.5-4b-q4.gguf \
  --host 127.0.0.1 --port 18773 -c 4096 --n-gpu-layers 0 \
  --jinja --chat-template-file /tmp/no_think_template.jinja
```

`--jinja --chat-template-file` 用于关闭内置的 `<think>` 模式，让模型在纯续写模式下工作。

---

## 待实现清单

- [x] L0→L1 每轮压缩 + raw_ppl
- [x] L1→L2 合并（PPL ≥ 5 过滤）
- [x] L2→L3 日常摘要（代码拼装）
- [x] L1→L4 高 PPL 直接归档
- [x] L3→L4 日常归档
- [x] 上文记忆视图 UI（各层展示 + 时间戳）
- [ ] L1 预处理第一阶段：LLM 判断"说的是不是我" + 替换称呼（硬币你→我等）
- [x] ~~L4 向量检索~~ → 已由【长期记忆】系统独立实现（见 `长期记忆/架构说明.md`，端口 18774）
- [ ] 工具调用循环处理：`[TOOL:搜索]` → webfetch → 结果回填 → 模型据此回复

---

## 数据流向（与其他系统关系）

```
麦克风/键盘 → ASR (asr+2d8.2确认/mic_vad_asr_streaming_vad.py) → POST /text
  → 管线中心 (18772) build_prompt → LLM 桥 (18771) → llama-server (8080) → 回复
  → history 追加
     ├─ 上文记忆: trigger_summarize() 每轮摘要归档 L1~L4（本系统）
     └─ 长期记忆: trigger_retrieval() 每轮检索召回（长期记忆/架构说明.md）
```

两个记忆系统并行工作：上文记忆负责**写**（压缩归档），长期记忆负责**读**（检索召回）。
