# 02 — 记忆系统（双记忆并行）

> 两个记忆系统并行工作：**上文记忆负责写**（压缩归档），**长期记忆负责读**（检索召回）。
> 文件：`输入中心/上文记忆/`、`输入中心/长期记忆/`

---

## 2.1 上文记忆 — L0~L4 压缩归档

**文件**：`输入中心/上文记忆/` | 架构文档：`上文记忆/架构说明.md`
**依赖**：摘要模型 4B（18773，Huihui-Qwen3.5-4B，CPU）
**设计依据**：模拟人类记忆层级化压缩（Cowan 工作记忆 4±1、Nature 逐字→要点→精髓、睡眠期巩固、多痕迹理论）

### 四层结构

```
L0 (原文)        5 轮完整对话（history 保留）
   ↓ 5→1 摘要化（纯文本 PPL 过滤）
L1 (段落摘要)    每条 = 1 轮压缩，带 raw_ppl
   ↓ 满 6 条合并最老 3 条（PPL ≥ 5 才保留）
L2 (会话摘要)    每条 = 3 个 L1 合并，带时间戳
   ↓ 满 6 条合并最老 3 条
L3 (日常摘要)    代码拼装：我和{参与者}进行了闲聊
   ↓ 直接归档
L4 (永久记忆)    来自 L3 + L1 高 PPL(>15) 条目
```

### 触发条件

| 转换 | 条件 | 处理 |
|------|------|------|
| L0→L1 | history > 10（5轮） | 取最早未处理一轮 → 4B 摘要 + raw_ppl |
| L1→L2 | mem_l1 ≥ 6 | 取最老 3 条，PPL<5 丢弃，4B 合并为 1 条 |
| L2→L3 | mem_l2 ≥ 6 | 取最老 3 条，4B 总结参与者 → "我和X进行了闲聊" |
| L1→L4 | raw_ppl > 15 | 直接归档，标记"重要事件" |
| L3→L4 | 每次生成 L3 | 直接归档，标记"日常" |

### L1 摘要预处理（第一视角）

```
对话[来源名]：xxx  →  {来源名}对我说：xxx     （强调对方在跟我对话）
我想： 中的来源名  →  有人说                  （避免模型混淆）
[TOOL:报错]xxx     →  我故意报错
[TOOL:回复]xxx     →  我说：xxx
[TOOL:搜索]xxx     →  我搜索：xxx
[TOOL:表情]/[结束] →  跳过
```

### PPL 过滤

- L1 存储：不过滤（全部标注）
- L1→L2：PPL ≥ 5（低于的日常内容丢弃）
- L1→L4：PPL > 15（极高 PPL 直接进永久）
- PPL = 摘要文本单独发 4B，`n_predict=0` 算 logprob 均值取 exp(-mean)

### 拼装成品（→ prompt 摘要模块）

`_build_final_context()` 实时拼 L0+L1+L2 → daemon 每秒推"拼装成品桥"(默认18000) → build_prompt 摘要模块读 → `<|im_start|>摘要\n[话题摘要][最近摘要][对话历史]<|im_end|>`。

> 2026-08-08 修复：`_push_loop` 之前只推 L0（对话历史），页面预览却是 L0+L1+L2——坏 AI 假代码。已抽共用函数 `_build_final_context()`，页面预览与推桥同源。

---

## 2.2 长期记忆 — BERT 分类 + embedding 检索

**文件**：`输入中心/长期记忆/` | 架构文档：`长期记忆/架构说明.md`、`长期记忆系统说明.md`
**依赖**：检索服务 18774（chinese-roberta-wwm-ext BERT 分类器 + bge-base-zh-v1.5 embedding）
**数据**：memories.db SQLite（1489 条，2026-08-01 重导）

### 检索流水线

```
LLM 回复完成 → history 追加
  ↓
trigger_retrieval()（pipeline_server.py）
  1. 取最新一轮 (user_raw + reply_full)
  2. build_l05()：parse_reply 拆想法/回复/工具 → 动作描述
     → L0.5: "{来源}对我说：xxx\n我想：xxx\n我说：xxx"
  3. 【本地】先填充 last_retrieval 的 l0/l05（不依赖服务）
  4. POST 18774/retrieve {user, thought, action}
  ↓
memory_retrieve_server.py (18774)
  1. BERT 分类 full_text → 全量标签（领域/规模/时间/评价）
  2. BERT 分类 coin_text → 硬币标签
  3. 重心位移 Δ = 硬币重心 - 全量重心（评价/规模/时间三轴）
     ※ 时间/规模轴 ×0.5 衰减（分类易波动），评价轴保持全量（真正的态度信号）
  4. 主移动 = |Δ| 最大的轴（|Δ|<0.05 视为无移动）
  5. 【只走主移动一步】只有主移动轴偏移一级，其他轴保持全量
  6. SQLite 标签过滤（严格→宽松 4 级降级）
  7. bge embedding 余弦 → top-3 候选
```

### 关键设计决策

1. **独立进程**：18774 独立于主管线（BERT+bge 加载慢不拖累启动，可单独重启）
2. **L0/L0.5 本地即时生成**：服务挂了前两块照常显示
3. **每轮实时处理**：不等待 batch
4. **重心位移衰减**：时间/规模 ×0.5（防带偏检索空间），评价保持全量
5. **只走主移动一步**：防多轴同时走跳到无数据组合（如"中等+过去"过滤掉感情线）
6. **防重复导入**：UNIQUE(text, domain) + INSERT OR IGNORE
7. **线程安全**：check_same_thread=False

### 两种标签格式（⚠️ 顺序相反，勿混）

```
db 导入格式:    [规模|时间|评价|领域]   例: [普通|现在|我喜欢|社交/关系]
BERT 训练格式:  [领域|规模|时间|评价]   例: [社交/关系|普通|现在|我喜欢]
```

### 数据存放

```
记忆源数据（.md 可编辑）: 长期记忆文件内容存放/伪造记忆/
  ├─ 人际关系/  50 个文件（01_拉姆 ~ 50_鸟山明）
  ├─ 知识条目/   7 个文件（ACG/编年史/哲学/健康/文学/历史/社交）
  └─ 关系补充条目.md  299 条→615 行
SQLite: 长期记忆/memories.db（1489 条）
BERT 训练数据: 桌面/聊天内容/（2955 条，10 领域）
模型: ~/coin_models/chinese-roberta-wwm-ext + domain_classifier/model.pt
```

---

## 2.3 已知缺口（待办）

- [ ] **检索结果注入 prompt**：目前只显示在页面，还没把命中记忆写进 memory 模块（下一步）
- [ ] 检索频率控制：每轮都检索，后续可加"间隔 N 轮/仅高 PPL 轮"节流
- [ ] 领域分类准确率 0.72（双领域标签噪声影响）
