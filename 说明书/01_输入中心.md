# 01 — 输入中心（管线核心）

> 文件：`输入中心/` | 架构文档：`pipeline_架构说明.md`、`SSE架构说明.md`
> 角色：所有"输入"汇入这里，拼成 prompt 发给主脑，处理回复并分发到各表达模块。

---

## 1.1 LLM 桥（18771）

**文件**：`mini_coin_llm_bridge.py` — 透明代理 + 数据中转

| 端点 | 方法 | 功能 |
|------|------|------|
| `/` | GET | 监控页（最新输入/输出） |
| `/` | POST | 接收 `{"prompt":"..."}`，转发 llama-server(8080) |
| `/text` | POST | 接收 `{"text":"..."}`，存到 `source_last` |
| `/source_last` | GET | 返回各源最新文本 |

**核心机制**：`source_last` 由桥维护——外部输入方（ASR/弹幕/手动）只 POST /text 写桥，管线中心轮询读。**输入方与消费方解耦**，改外部脚本无需动管线。

---

## 1.2 管线中心（18772 + 内置桥 18000-18009）

**文件**：`pipeline_server.py` — 核心拼装引擎 + Web 管理界面

### 模块系统（prompt 拼装的基本单元）

每个模块 = 一段 prompt 片段：

| 字段 | 说明 |
|------|------|
| `id` / `label` | 唯一标识 / 显示名 |
| `format_start/end` | 格式包裹（如 `<|im_start|>记忆\n` / `<|im_end|>`） |
| `enabled` | 是否启用 |
| `content` | 静态内容（可选） |
| `sources` | 数据源列表 `[{name, endpoint, active}]`（读桥） |
| `show_src` | 是否显示源名（chat 模块固定 `对话[源名]：`） |

### 内置模块（默认状态）

| 模块 | 默认 | 数据源 | 说明 |
|------|------|--------|------|
| 系统提示 | 开 | 固定 | 身份 + 工具格式 |
| 记忆 | 开 | 静态/检索桥 | 长期记忆检索结果（**在输入前，引导输出**） |
| 历史摘要 | 关 | 拼装成品桥 | history>8 轮自动摘要（含 [对话历史]） |
| 对话输入 | 开 | 拉姆 18771 | `对话[源名]：内容` |
| 弹幕输入 | 开 | 弹幕桥 18008 | 弹幕行直接注入 |
| 视觉输入 | 关 | 截图桥（预留） | — |
| 工具返回 | 关 | 工具桥（预留） | 搜索结果回填 |
| 情绪指引 | 关 | FACS 桥（预留） | 实时情绪 |
| 提示词 | 关 | 静态 | 额外指令 |

### 最终 prompt 结构（与训练对齐，2026-08-09）

```
<|im_start|>system
身份+工具格式<|im_end|>
<|im_start|>摘要
[话题摘要]     ← L2（有则）
[最近摘要]     ← L1（有则）
[对话历史]     ← L0：history 分行文本（轮次间空行，保留 5 轮）
<|im_end|>
<|im_start|>记忆
{长期记忆检索结果}<|im_end|>     ← 输入前，引导输出
<|im_start|>输入
对话[拉姆]：当前输入<|im_end|>
<|im_start|>硬币
```

> ⚠️ 重要：history 不是逐条 `<|im_start|>输入/硬币` 交替，而是 `[对话历史]` 标签 + 分行文本整段（`_build_final_context` 拼装）。v9d 训练已按此对齐。

### 输入触发链（ASR 驱动）

```
外部输入方 → POST 桥 /text（只写桥）
  ↓
_check_trigger_sources 每秒轮询触发源桥（弹幕+chat 全部源+自动化注入桥）
  → 任一非空 → _mark_pending()（幂等）
  ↓
_send_loop：冷却到点（5s）→ _do_send() 统一发送
```

### LLM 发送状态机（2026-08-08）

```
LLM 两态：可对话 / 不可对话（5s 冷却）
  发送一次 → 冷却（不可对话）
  冷却期内：触发源输入只拼进 prompt，压住发送信号
  冷却到点 + 有积压 → 合并一次发送（对话+弹幕+系统都进 prompt）
  冷却到点 + 30s 无发送 → 自动化注入系统提示 → 自言自语
```

### L0 哲学（2026-08-08）

**history = LLM 真实看见过的东西**。一句话没进 LLM（发送失败/冷却期被覆盖）= 不存在。
- user 轮**只在 _do_send 发送成功后**记录（清桥前读触发源桥拼 `对话[源名]：内容`、弹幕原文）
- 发送失败 → 不记录、桥留着重试
- 冷却期多条输入被覆盖 → 只有最终真发出去的那条算数

### 下桥清洗 `_sanitize_reply`（2026-08-08）

LLM 回复下桥**第一步**，history/上文记忆/检索记忆全基于干净回复：

1. **删复读**：尾巴复读(≥4次)/6x 任意重复 → 从复读开始整段删光（`REPEAT_WHITELIST` 语气词白名单放行）
2. **工具 arg 非法内容**：`[TOOL:xxx]` 内任何非 `[TOOL]` 闭合结构 → 删到合法闭合前
3. **未闭合工具** → 删到结尾 + 末尾补 `[TOOL]`
4. **孤立 `[TOOL]`** → 删除，前后文本保留

复读检测思路来自训练侧 UL 惩罚函数（推理侧落地）。

### 输出侧 VAD 转发（2026-08-08 修复）

`_do_send` 清洗后、进队列前，把回复转发给 FACS /output（方案 B）：
```python
_vad_text = reply.split('[TOOL:')[0].strip()        # thought（内心戏）
# 若有 [TOOL:回复] → 拼接其纯文本 arg
# POST http://localhost:18768/output {"text": _vad_text}
```
驱动"我说我/我说你"输出侧表情（此前 LLM 回复忘记转发 VAD，断点已修）。

---

## 1.3 输出处理队列

LLM 回复到达时 `_push_reply_to_queues` 正则解析工具，按类型入队：

| 队列 | 内容 | done 机制 | 消费 |
|------|------|-----------|------|
| 表达队列 | 回复/表情 | 轮询桥 tts_done | 回复→TTS(8000) SSE；表情→18770 push |
| 执行队列 | 搜索/结束 | 轮询桥 tts_done | 搜索→18775 SSE |
| 报错队列 | 报错 | 间隔计时(3s) | 拷贝固定 WAV → 播放 |

搜索完成 → 结果上"结果桥" + done 上桥 → 工具返回模块注入 prompt。

---

## 1.4 SSE 架构（跨语言可复用模式）

```
发送方(Python) → POST /api/queue → 桥服务 → GET /events (SSE) → 接收方(JS/Py)
```

- **发送方**：HTTP POST 丢进桥，不关心谁在听
- **桥**：入队 + SSE 推送所有在线客户端
- **接收方**：EventSource 收到 → 自动处理

Python 读 SSE 正确姿势：`codecs.getincrementaldecoder('utf-8')` 增量解码（半字不丢，中文安全）。
错误写法：逐字节 `decode(errors='ignore')` 会丢中文。

**弹幕桥弃用 SSE 原因**（2026-08-08）：
1. 长连接状态管理坑（socket 超时无法再读）
2. 维护成本高（队列+推送+心跳+断线清理）
3. 推即弃无重放

→ 改用**读后清空（wait 哨兵）**：消费者每 0.5s `GET /take`（原子"返回内容+置wait"），读到 wait 即无新消息。单消费者天然不重复、无连接状态、断线零成本。

---

## 1.5 桥系统（内置 18000-18009）

管线中心启动时自带 10 个桥（同进程 daemon 线程），`BaseHTTPRequestHandler`：
- `POST /text` 存文本（source 名区分）
- `GET /source_last` 返回所有源
- `GET /take` 原子读清（弹幕用）
- wait 语义：读认 wait（空桥）、清置 wait（不删 key）

桥管理页可一键检测连通状态 + 查看占用（系统/内置/模块配置三类）。

---

## 1.6 配置持久化

| 机制 | 说明 |
|------|------|
| 自动保存 | 每次修改实时写入 `pipeline_config.json` |
| 手动保存 💾 | 生成 `pipeline_config_YYYYMMDD_HHMMSS.json` |
| 启动加载 | 自动读取配置文件，保留上次顺序 |
