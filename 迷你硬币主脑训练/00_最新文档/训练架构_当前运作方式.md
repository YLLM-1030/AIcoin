# 迷你硬币训练架构 — 当前运作方式（v9d）

> 2026-08-09 | 基于 `新建训练/grpo_tool_v9d.py`（最后使用版本，07-23）
> 产出检查点：`ckpt_tool_main_v11b`（当前推理用的模型）

---

## 一、训练流水线（全历程）

```
基座 Qwen3-8B
  └→ 🅐 持续预训练 PT — 身份语料 + 公理体系 + 叙事数据 (v1~v53)
       └→ 🅑 身份 SFT — 自我认知问答对
            └→ 🅒 工具身份 SFT — 教会工具调用格式
                 └→ 🅓 工具链 GRPO — 学会回复/搜索/表情/报错/结束五工具
                      └→ 🅔 续写 GRPO (v3a) — 学会工具后继续思考 → 再调用工具 → 结束
                           └→ 🅕 搞事 GRPO (v9d, 当前) — history/memory 格式 + 人格强化
```

**检查点链**：`ckpt_tool_main_v3a → ... → v11a → v11b`（v11b = 当前推理模型）

---

## 二、v9d 脚本结构（最后版本）

| 项 | 值 |
|---|---|
| 文件 | `新建训练/grpo_tool_v9d.py` |
| 基座 | `ckpt_tool_main_v11a` |
| 输出 | `ckpt_tool_main_v11b`（Epoch2 中间存 `v11b_mid`） |
| 数据 | `等待挖掘的语料/搞事/Neuro搞事_春药篇_final.txt` |
| GROUP_SIZE | 2（1 采样 + 1 golden） |
| LR / 优化器 | 2e-6 / **Lomo（全参数，无 LoRA）** |
| Epoch | 4（shuffle） |
| MAX_NEW_TOKENS | 500 |
| 解码 | do_sample, temp=0.9, top_p=0.9 + ToolBiasProcessor |

---

## 三、数据格式（v9d 新格式：history/memory/input/output）

```
{
--- 数据1 ---
history:                      ← 多轮对话上文（可选）
对话[拉姆]：xxx
硬币回复（thought + [TOOL]）
对话[拉姆]：yyy
硬币回复（thought + [TOOL]）

memory:                       ← 长期记忆（可选，插在 history 和 input 之间）
我记得有一次……（记忆内容）

input:                        ← 当前输入
对话[拉姆]：当前问题

output:                       ← 目标输出（thought + [TOOL]）
硬币的目标回复
}
```

### 解析方式（v9d L38-81）
- 大括号切块：`re.findall(r'\{([\s\S]*?)\}', content)`
- 按 `history:`/`memory:`/`input:`/`output:` 行切字段
- history 转 messages：
  - `对话[xxx]：`/`弹幕[xxx]：`/`系统[xxx]：` → user 行
  - 后续非空行（硬币 thought+[TOOL]）→ assistant 行
- 缺 input 或 output 的块跳过

---

## 四、Prompt 拼装 + memory 注入（关键）

```python
messages = [{"role": "system", "content": TOOL_PROMPT}]
for h in hist_msgs: messages.append(h)          # history 多轮
if mem: messages.append({"role": "memory", "content": mem})   # ← 记忆段
messages.append({"role": "user", "content": inp})             # 当前输入
```

**自定义 chat_template**（user→输入, assistant→硬币, memory→记忆）：
```
<|im_start|>输入\n...
<|im_end|>
<|im_start|>硬币\n...
<|im_end|>
<|im_start|>记忆\n{记忆内容}
<|im_end|>
<|im_start|>输入\n{当前输入}
<|im_end|>
<|im_start|>硬币\n          ← add_generation_prompt
```

**设计意图**：memory 作为独立 `<|im_start|>记忆` 段插在 history 与 input 之间 → 训练模型"看到记忆段 → 在回答中调用记忆内容"。这是"让 LLM 注意记忆提示"的核心机制。

---

## 五、训练循环（GRPO + UL）

### 每步流程（L238-358）
1. **拼 prompt**（system + history + memory + input）
2. **采样续写**（GROUP_SIZE-1 条，temp 0.9）→ golden 放末尾
3. **奖励**：golden +20，sampled +0 → 算 advantage（z-score）
4. **GRPO loss**（L290-328）：
   - golden：整个答案 ×5
   - sampled：整个答案 ×1
   - `loss = adv_dir × |adv| × loss_val`（prompt 部分 mask，只算续写）
   - backward + clip_grad 1.0
5. **UL 复读惩罚**（L330-355）：只对采样续写
   - 检测：尾巴复读 / 前向复读（"是。是"等）/ 6x 任意重复
   - 惩罚：对复读 token 做 `-log(1-p)` ×40

### Loss 结构总结
| 项 | 权重 |
|---|---|
| golden 续写 | ×5（v9d） |
| sampled 续写 | ×1 |
| UL（复读惩罚） | ×40 |
| prompt/history/memory | mask（-100，不参与 loss） |

---

## 六、推理侧 LogitsProcessor（防 TOOK 复读）

```python
class ToolBiasProcessor(LogitsProcessor):
    # 检测到 [TO(58,5207) 后：推 OL(1930) +10，压 OK(3925) -10
    # 防止生成 [TOOK 陷入复读循环
```

---

## 七、特殊训练方式要点（"很多优化"）

1. **Lomo 全参优化**：不用 LoRA，全参数训练（内存友好）
2. **GRPO + golden 对比**：GROUP_SIZE=2，golden 高权重拉模型贴近目标行为
3. **UL 复读惩罚**：三种复读检测，训练中实时惩罚，防模型陷入复读循环
4. **memory 段注入**：新格式支持长期记忆引导（v9d 的核心新增）
5. **自定义 chat_template**：输入/硬币/记忆三段式，贴合 pipeline 的 prompt 格式
6. **ToolBiasProcessor**：推理时 logits 干预防 TOOK
7. **enable_thinking=False**：Qwen3 不插入 `<think>`（模型自己用中文 thought 前置）

---

## 八、文件位置索引

| 文件 | 位置 |
|---|---|
| v9d 训练脚本 | `Desktop/新建训练/grpo_tool_v9d.py` |
| v9d 备份 | `Desktop/新建训练/grpo_tool_v9d_backup.py` |
| 搞事数据 | `Desktop/等待挖掘的语料/搞事/Neuro搞事_春药篇_final.txt` |
| v11b 检查点 | `Desktop/ckpt_tool_main_v11b` |
| 推理测试脚本 | `Desktop/新建训练/agent_test_manual.py` |
| 旧版工具链脚本 | `迷你硬币主脑训练/02_训练脚本/GRPO/grpo_tool_main_v3a.py`（v3a，07-14 已过时） |

> 注：`迷你硬币主脑训练` 文件夹停留在 v3a 时代（07-14），v9d 之后的训练都在 `新建训练/` 进行——该文件夹需要同步更新。
