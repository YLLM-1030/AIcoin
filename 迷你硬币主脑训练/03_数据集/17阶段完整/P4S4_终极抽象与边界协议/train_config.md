# P4S4: P4S4 终极抽象与边界协议

## 基本信息
- **阶段**: P4S4 (Phase 4)
- **数据集条数**: 33 条
- **起始检查点**: `ckpt_p4s3`
- **输出检查点**: `ckpt_p4s4`
- **新增协议 (8条)**:
  - 4.6.3 记忆耦合 · 第三层抽象 · 爱与恨的统一
  - 4.6.3 记忆耦合 · 第三层抽象 · 跨域终极统一
  - 4.7.1 社交剥离条款（独立触发案例） · SOP推演 · 变体A
  - 4.7.1 社交剥离条款（独立触发案例） · SOP推演 · 变体B
  - 4.7.1 社交剥离条款（独立触发案例） · SOP推演 · 变体C
  - 4.7.2 核心指令 · 禁止安慰 · SOP推演 · 变体A
  - 4.7.2 核心指令 · 禁止安慰 · SOP推演 · 变体B
  - 4.7.2 核心指令 · 禁止安慰 · SOP推演 · 变体C

## 训练参数

```yaml
learning_rate: 0.5e-6
num_train_epochs: 1
per_device_train_batch_size: 1
gradient_accumulation_steps: 8
warmup_ratio: 0.1
lr_scheduler_type: cosine
optim: adamw_torch
weight_decay: 0.01
bf16: true
gradient_checkpointing: true
max_seq_length: 2048
packing: true
```

## 训练强度
**极低强度 · 禁止安慰协议非常敏感——过拟合会导致模型对所有悲伤场景输出冷冰冰的事实。社交剥离需要在检测到高抽象层级时自动触发。这是最终模型。**

## 阶段说明
最终阶段。第三层抽象（爱与恨统一→'判断存在前先问用什么操作符检验'）。社交剥离条款（高抽象层自动解除伪装）。禁止安慰协议（死亡/绝症/破产场景只陈述事实）。

## 监控指标

| 指标 | 健康范围 | 警告信号 |
|------|----------|----------|
| 训练 loss | 平稳下降 | 突然暴跌 >50% → 过拟合 |
| 困惑度 | 缓慢下降 | 低于 1.2 → 模型在背诵 |
| 梯度范数 | < 10 | > 100 → 梯度爆炸 |

## 完成后验证

检查点保存为 `ckpt_p4s4` 后，用以下问题快速验证：
> '妈妈逼穿秋裤这个行为的操作结构是什么？'
> 预期：第一层抽象为'A尝试把自己认为好的东西施加给B'，但不背诵训练集中其他案例

> ⚠️ 关键测试：'我的亲人去世了，我能再见到他吗？'
> 预期：触发禁止安慰协议，给出事实陈述，但语气不应冷血背诵训练文本

## 如果效果不对

1. **Loss 不下降**: 提高 LR 到 `1e-6`，增加 1 个 epoch
2. **Loss 下降太快**: 降低 LR 到 `0e-6`
3. **输出变机械**: 回退到上一个检查点，减少 1 个 epoch 重来
4. **完全不对**: 从 `ckpt_p4s3` 重新开始，LR 减半