# 迷你硬币主脑训练 — 整理说明（2026-08-09）

> 本次整理：把散落在 桌面/新建训练/训练数据/右上角minicoinB2/ 的训练内容按主题复制并入本文件夹。
> 原文件保留，本目录是**汇总归档**。

---

## 一、训练时间线速查（完整版见 说明书/06_训练系统.md）

```
① 17阶段哲学训练（SFT，5月底-6月初）→ ckpt_merged_54ep (=P4S4_v7)
② b 系列人格训练（SFT，6-13~6-17）→ minicoin b2
③ R1 预训练（6月初，think 三段推理分支）
④ v1-v53 持续预训练 PT（6-30 叙事化改造定稿）→ ckpt_pt_v53 → ckpt_pt_final_v3
⑤ 身份 SFT（7-11，sft_lm.py："PT教知识，SFT教角色"）→ ckpt_sft_step1_identity
⑥ 工具身份 SFT（7-11，convert_toolformer）→ [TOOL:] 工具格式基座
⑦ 工具链 GRPO v1~v10（7-16~7-19）→ v8/v9a/v9b/v10a
⑧ 续写 GRPO v3a（7-16 数据）→ v11a 前身
⑨ 搞事数据（7-20 格式规则定型）→ 40%/60% 数据（7-22）→ v9d 脚本（7-23）→ v11b
⑩ 记忆系统（7-24~8-01）BERT 分类器 + 记忆库重建
⑪ 记忆引导强化（8-08）+ 缺口数据（8-08/09）→ 新老合并 523 条 → v9d 改造 → v12b（训练中）
```

---

## 二、本次并入内容映射

### 00_最新文档/（当前版本）
| 文件 | 来源 |
|------|------|
| `grpo_tool_v9d.py` | `新建训练/grpo_tool_v9d.py`（当前训练脚本，v11b→v12b） |
| `训练架构_当前运作方式.md` | `新建训练/训练架构_当前运作方式.md` |

### 02_训练脚本/
| 目录 | 来源 |
|------|------|
| `b系列/` | minicoinB2 训练经验/write_b1.py write_b15.py write_b15p.py + full_finetune.py + 待分类/write_b*.py + 第二次训练的数据/*.json |
| `记忆系统/` | 输入中心/长期记忆/train_bert.py import_memories.py |
| （已有 GRPO/ PT/ SFT/ Stage_Runner/ 不动） | |

### 03_数据集/（新增 8 个主题目录）
| 目录 | 内容 | 来源 |
|------|------|------|
| `b系列训练数据/` | b0.2~b4.0 + negation | 新建训练/train_data/ |
| `搞事数据/` | Neuro搞事_春药篇系列 + 凛傲娇系列 + 账号/钱类搞事 + 单轮/多轮改造 | 桌面/好像没用 不确定/等待挖掘的语料/搞事/ |
| `工具链GRPO数据/` | 40%/60%数据 + 再训练 + 续写_混合 + 多轮对话 + 自言自语展开链 + 备份7.16 | 桌面 txt |
| `v9d_新老合并/` | 新老合并.txt + 记忆引导强化(351/187/164) + 记忆改造示例v1-v3 + 记忆素材 | 桌面 + 训练数据/ |
| `全新缺口数据/` | 越描越黑/挑逗/处境说反/八卦/讨价还价/日常 + 参考素材 + 生成方法说明 | 训练数据/全新缺口数据/ |
| `记忆强化引导整理/` | 记忆强化引导_数据1-187（19文件 + 合并版） | 训练数据/新的记忆强化引导数据整理/ |
| `17阶段完整/` | P1S1~P4S4 完整 17 阶段（train_config + train_data） | 右上角/待分类/01_训练数据/17阶段训练数据/ |
| `记忆系统训练/` | 人际关系/知识条目/新闻事实/聊天内容/关系补充 | 输入中心/长期记忆/训练数据/ |

### 08_日志与记录/
| 文件 | 来源 |
|------|------|
| `2026-06-14_训练总结.md` | minicoinB2 训练经验（b1/b1.5/b2 复盘） |

### 01_架构文档/（补充）
| 文件 | 说明 |
|------|------|
| `拉姆设定集_txt格式.txt` | 拉姆人格背景设定（原 桌面/txt格式.txt） |
| `ai项目助手prompt.txt` | 排错铁律（查经验包→搜社区→打日志→确认权限） |
| `用户输入优先级警告_解决.txt` | 用户输入优先级 > 系统提示词 |

---

## 三、注意

1. **未复制**：模型权重（model.safetensors / memories.db / checkpoint）——体积大，留在原位置
2. **当前活跃工作区**：训练在 `桌面/新建训练/` 直接跑（脚本头部参数），本目录是归档
3. **详细架构**：`新架构硬币/说明书/06_训练系统.md`（完整时间线 + PT/SFT/GRPO 各阶段细节）
